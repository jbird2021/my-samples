- name: community_kubernetes.yml
  hosts: all
  gather_facts: False
  become: False
  tasks:   
  - name: Ensure the myapp Namespace exists.
    community.kubernetes.k8s:
      api_version: v1
      kind: Namespace
      name: myapp
      state: present
