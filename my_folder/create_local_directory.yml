- name: create_local_directory.yml
  hosts: localhost

  tasks:
    - name: Create a directory if it does not exist
      ansible.builtin.file:
        path: /staging/demo_project
        state: directory
        mode: '0755'
        # delegate_to: localhost
