cat vars/credentials.yml
---
creds:
  - name: Gokulan-Github-Credential
    type: Source Control
    state: present
    #Below is my PAT key
    password: test
    username: test
    org: Default

orgs:
  - name: Gokulan-Test1
    description: This is a test organization created by CaC-Gokulan and can be deleted
    state: present
