- name: rebot server
  hosts: all
  gather_facts: True
  become: True
  tasks:   
    - name: Reboot a slow machine that might have lots of updates to apply
      ansible.builtin.reboot:
        reboot_timeout: 3600
