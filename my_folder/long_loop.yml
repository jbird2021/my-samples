- hosts: localhost
    
  gather_facts: no
  vars:
    echo_list:
      - "one"
      - "two"
      - "three"
  tasks:
    - name: print items
      debug: 
        msg:
          - "{{ item }}"
      loop: "{{ echo_list }}"
